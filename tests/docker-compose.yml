# Use root/secret as user/password credentials
version: '3.1'

services:

  db:
    image: mariadb
    environment:
      MYSQL_DATABASE: chat-test
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3306:3306"

  redis:
    container_name: redis
    image: redis
    ports:
      - "6379:6379"
